<%= render "navbar" %>

<%= link_to 'Back', "/grades/#{@grade_id}/activities", class: "btn btn-info btn-sm" %>

<div class="form">
  <h1>Editing Reading</h1>

  <%= render 'form', reading: @reading %>
</div>
